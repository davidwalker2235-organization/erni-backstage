app:
  # Should be the same as backend.baseUrl when using the `app-backend` plugin
  baseUrl: http://localhost:3000

backend:
  baseUrl: http://localhost:7007
  listen:
    port: 80
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  database:
    client: pg
    connection:
      host: ernibackstagepsql.postgres.database.azure.com
      port: 5432
      user: backstage@prodernibackstagepsql
      password: "Dacarma@9211"
      ssl: true
  cache:
    store: memory

integrations:
  github:
    - host: github.com
      apps:
        - appId: 828233
          clientId: Iv1.070a80f1762cab18
          clientSecret: Ffb6c39a026e53dba549affdb09170aa2ab89809
          webhookUrl: https://0d070862-c442-44f9-8ea9-73eaa403522a.webhook.we.azure-automation.net/webhooks?token=CSORBUWS7ljxtYTBQV6rTlfBOMkDtGSMKnfJODUzyZ4%3d
          webhookSecret: Dacarma@9211
          privateKey: SHA256:ArDIfCq4EDbIpWHAsKp065K7lnzUzAxsJxT/d4ru0gw=
auth:
  environment: production
  providers:
    microsoft:
      production:
        clientId: ${{ secrets.AZURE_CLIENT_ID }}
        clientSecret: ${{ secrets.AZURE_CLIENT_SECRET }}
        tenantId: ${{ secrets.AZURE_TENANT_ID }}

techdocs:
  builder: 'external'
  publisher:
    type: 'azureBlobStorage'
    azureBlobStorage:
      containerName: ${{ secrets.TECHDOCS_CONTAINER_NAME }}
      credentials:
        accountName: ${{ secrets.TECHDOCS_STORAGE_ACCOUNT }}
